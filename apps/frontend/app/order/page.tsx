import React from 'react'

function Order() {
  return (
    <div>
      Tabs:
      Open Orders (limit orders not filled)
      Trade History (filled orders)
      All Orders
      Orders table:
      Asset
      Type (Buy/Sell)
      Price
      Quantity
      Total
      Status (Filled, Cancelled, Pending)
      Timestamp
      Search & filter (by asset, date, type)
      Cancel button (for open orders)
    </div>
  )
}

export default Order